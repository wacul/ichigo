version: "2"
linters:
  enable:
    - bodyclose
    - depguard
    - dogsled
    - gocritic
    - godox
    - revive
    - misspell
    - nakedret
    - prealloc
    - stylecheck
    - unconvert
    - unparam
    - whitespace
  settings:
    gocritic:
      disabled-checks:
        - elseif
formatters:
  enable:
    - gofmt
    - goimports
